/** Fixed box for company logos in cards/grids; images use object-contain inside this slot. */
export const COMPANY_LOGO_MARK_BOX_PX = {
  width: 88,
  height: 28,
} as const;

/** Slightly smaller slot for case study cards / detail header. */
export const CASE_STUDY_LOGO_MARK_BOX_PX = {
  width: 72,
  height: 22,
} as const;
